// Função que inicia a animação da lua
window.onload = function() {
    const lua = document.querySelector('.lua');
    // A lua começa a orbitar assim que a página carrega
    lua.classList.add('orbitando');
};
